# --- Docker Compose Configuration ---
# Postgres container settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=trading_db

# Database URL for App/ETL containers
# When running with docker-compose, the host is the service name 'db'
DATABASE_URL=postgresql://postgres:password@db:5432/trading_db

# --- Local Development Configuration (Optional) ---
# If running locally without Docker Compose, you can use these or set DATABASE_URL to localhost
DB_USER=postgres
DB_PASS=password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trading_db
